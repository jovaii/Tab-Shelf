# Tab Shelf Release Progress

## Current State

`SPEC-FROZEN`

The App Store and GitHub release design was approved on 24 August 2026 and committed as `1c06e1d`. Product implementation has not started.

## Budget Ledger

| Phase | Target | Current use | Stop signal |
| --- | ---: | ---: | --- |
| Intake and specification | 5–8k tokens | Complete | Commercial or privacy direction becomes ambiguous |
| Implementation | 15–25k tokens | Not started | Work expands beyond the approved release design |
| Focused verification | 8–12k tokens | Not started | The same failure repeats without new evidence |
| Apple and GitHub delivery | 8–15k tokens | Not started | Account, signing, or remote state cannot be verified |
| Debug reserve | 0–10k tokens | Unused | No new hypothesis supports another attempt |

## Evidence Ledger

### Repository state

FACT: The working repository is the linked worktree on branch `feature/independent-v1` at commit `1c06e1d46244024552650c1f7ee7a061ba82fe28`.
SOURCE: `git rev-parse --show-toplevel`, `git branch --show-current`, and `git rev-parse HEAD`.
TIME: 24 August 2026.
HYPOTHESIS: This worktree can safely contain the release implementation without altering another checkout.
NEXT: Implement only after the written plan is complete.
SUCCESS: Each bounded task commits independently on this branch.

### Native project generation

FACT: `scripts/package-macos.sh` creates the Xcode project with Apple's Safari packager, fixes the two Jovaii bundle identifiers, builds Release, ad-hoc signs the App, verifies it, and includes the three legal files.
SOURCE: Direct inspection of `scripts/package-macos.sh` and `tests/macos-package-contract.test.mjs`.
TIME: 24 August 2026.
HYPOTHESIS: A shared deterministic preparation step can support local packaging and App Store archiving without maintaining generated Xcode files.
NEXT: Add contract tests before introducing the preparation step.
SUCCESS: Both paths use the same tracked host templates and project configuration rules.

### Sandbox and network state

FACT: The generated host and extension targets have `ENABLE_APP_SANDBOX = YES`; the host target also has `ENABLE_OUTGOING_NETWORK_CONNECTIONS = YES` in Debug and Release.
SOURCE: Direct inspection of `native/generated/Tab Shelf/Tab Shelf.xcodeproj/project.pbxproj` generated by Xcode 26.6.
TIME: 24 August 2026.
HYPOTHESIS: Disabling the host network entitlement is required to align the packaged App with the approved no-first-party-network privacy contract.
NEXT: Add a failing project-preparation test that requires both host configurations to use `NO`.
SUCCESS: The generated project and archived candidate contain no outgoing-network entitlement.

### Product core

FACT: The extension uses only `tabs` and `storage`, contains no third-party runtime dependency, and has an existing dependency-free check command.
SOURCE: `extension/manifest.json`, `package.json`, and repository tests.
TIME: 24 August 2026.
HYPOTHESIS: Commercial distribution can reuse the existing extension without a feature fork or backend.
NEXT: Preserve the extension files and add release-contract tests around them.
SUCCESS: GitHub source and App Store candidate use the same versioned extension resources.

## External Gates

- GitHub remote mutation is deferred until local implementation and QA pass and the exact repository, branch, commit, commands, impact, rollback, and cost are presented for approval.
- Apple signing, archive upload, pricing activation, and App Review submission are deferred until the enrolled Apple team and App Store Connect state are verified and the exact external action is approved.
